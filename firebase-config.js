/* ============================================================
   FIREBASE CONFIG — connects the Tribute Wall to shared storage
   ============================================================

   Right now this file is EMPTY, so the site runs in "preview mode":
   tributes typed on this page are only saved in each visitor's own
   browser (using localStorage) and nobody else can see them.

   To make tributes, likes and comments truly shared — so every
   visitor sees the same wall — follow the 10-minute setup in
   SETUP.md, then paste your Firebase project's config below.

   Example (do not use these values, they are illustrative only):

   window.FIREBASE_CONFIG = {
     apiKey: "AIzaSyD-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
     authDomain: "mama-nwa-afo-memorial.firebaseapp.com",
     projectId: "mama-nwa-afo-memorial",
     storageBucket: "mama-nwa-afo-memorial.appspot.com",
     messagingSenderId: "123456789012",
     appId: "1:123456789012:web:abcdef1234567890abcdef"
   };

   Leave the line below as `null` until you have real values.
   ============================================================ */

window.FIREBASE_CONFIG = null;
